# 博客维护指南

本仓库：`D:\omp\blog` → https://github.com/zhengxianhanshu/zhengxianhanshu.github.io
站点：https://zhengxianhanshu.github.io/ （Hexo 8 + particlexf 主题）

## 一、修改后如何推送上线

用 GitHub Pages Actions 部署：**推送到 `main` 即自动构建上线**，云端跑 `hexo generate`。
`public/`、`db.json`、`node_modules/` 已在 `.gitignore`，不用本地生成、不用 `hexo deploy`。

```bash
cd /d D:\omp\blog
git add -A
git commit -m "feat: 描述你改了什么"
git push origin main
```

盯部署状态（约 30 秒）：

```bash
gh run list --limit 1     # 看最新一次运行
gh run watch              # 实时跟踪直到完成
```

完成后访问 https://zhengxianhanshu.github.io/ 确认；若 404 多半是 CDN 缓存，等几秒强刷（Ctrl+F5）。

## 二、如何写文章

### 1. 新建

```bash
npx hexo new "我的第一篇文章"
```

生成 `source/_posts/我的第一篇文章.md`（模板见 `scaffolds/post.md`）。

### 2. front matter（参照 `source/_posts/Hello-World-0.md`）

```markdown
---
title: 我的第一篇文章
date: 2026-09-12 15:30:00
tags:
  - hexo
categories:
  - 随笔
description: |
  首页摘要文字。
comments: true
toc: true
---
```

### 3. 本站已支持的语法

| 功能 | 写法 |
| ---- | ---- |
| 标注框 | `{% note tip %}` … `{% endnote %}`（支持 tip/info/warning） |
| 行内公式 | `$E=mc^2$`（KaTeX） |
| 块级公式 | `$$` 单独成行包裹 |
| Mermaid 图 | `<div class="mermaid">graph LR; A-->B</div>` |
| 代码块 | 标准 ` ```语言 `；高亮由主题浏览器端完成，Hexo 内置高亮已关闭（勿改 `_config.yml` 的 `syntax_highlighter`） |
| 配图 | 放入 `source/images/文章名/`，正文绝对路径引用：`![说明](/images/my-post/fig1.png)` |

### 4. 本地预览

```bash
npx hexo server        # http://localhost:4000 ，改文件即时刷新，Ctrl+C 停止
```

### 5. 发布

回到第一节 `git add -A && git commit && git push`。

## 三、常用命令备忘

| 目的 | 命令 |
| ---- | ---- |
| 新文章 | `npx hexo new "标题"` |
| 草稿 | `npx hexo new draft "标题"`（存 `_drafts/`，server 默认不渲染） |
| 发布草稿 | `npx hexo publish draft "标题"` |
| 新页面 | `npx hexo new page "about"` |
| 清缓存 | `npx hexo clean`（构建结果诡异时先跑） |
| 本地构建 | `npx hexo generate` |
| 头像/背景 | `source/images/avatar.png`、`background.jpg`，路径配置在 `_config.particlexf.yml` 顶部（avatar 兼作 favicon） |
| 菜单 | `_config.particlexf.yml` 的 `menu:` 块；`Demos` 项指向手工页 `/demos/sine/` |
| 手工 HTML 页 | 放 `source/demos/**`（`_config.yml` 已配 `skip_render`，原样透传） |
| 评论 | giscus：在 `_config.particlexf.yml` 的 `giscus:` 填 repo/repoID/category/categoryID 后置 `enable: true`（需先在 https://giscus.app 生成，并安装 app 到仓库） |

## 四、目录速览

```
_config.yml              站点主配置（url、菜单无关项、feed、search、skip_render）
_config.particlexf.yml   主题覆盖配置（头像、背景、菜单、卡片、giscus）
source/_posts/           文章 Markdown ←写这里
source/images/           图片资源（avatar.png / background.jpg / loading.gif）
source/demos/            自包含 HTML demo（不被 Hexo 渲染，原样输出）
themes/particlexf/       主题（第三方，尽量少改）
.github/workflows/deploy.yml  Pages 自动部署
```
